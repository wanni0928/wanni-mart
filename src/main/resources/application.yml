spring:
  profiles:
    active: local
  application:
    name: wannimart
  messages:
    basename: i18n/messages
    encoding: UTF-8
    cache-duration: PT1H
jwt:
  token:
    header: api_key
    issuer: wanni_market
    clientSecret: Rel3Bjce2MajBo09qgkNgYaTuzvJe8iwnBFhsDS5
    expirySeconds: 6000
---
#local 환경
spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/wannimart?serverTimezone=Asia/Seoul
    username: wanni
    password: wanni0928
#  h2:
#    console:
#      enabled: true
#      path: /h2-console
#  datasource:
#    platform: h2
#    driver-class-name: org.h2.Driver
#    url: "jdbc:h2:mem:wannimart;MODE=PostgreSQL"
#    username: sa
#    password:

  jpa:
    hibernate:
      show-sql: true
      ddl-auto: update
      use-new-id-generator-mappings: false
    properties:
      hibernate:
        format_sql: true
        hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        hibernate.hbm2ddl.import_files_sql_extractor: org.hibernate.tool.hbm2ddl.MultipleLinesSqlCommandExtractor
#platform:
#  org:
#    hibernate:
#      dialect: PostgreSQL95Dialect

logging:
  level:
    org.hibernate.SQL: debug # logger 로 출력
    org.hibernate.type: trace # 쿼리 파라미터 로그 출력

---
#dev 환경
spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    org.hibernate.SQL: info # logger 로 출력
    org.hibernate.type: trace # 쿼리 파라미터 로그 출력
---